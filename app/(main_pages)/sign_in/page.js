import React from "react";

function Page() {
  // on update store add a success modal and on error
  //  add error and also update the store such that when you call or invalidate
  //  query for Outlets it also automatically update the store's store
  return <div>page</div>;
}
export default Page;
